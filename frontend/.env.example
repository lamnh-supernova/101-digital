# Browser-reachable base URL of the SimpleInvoice API (backend/).
# Must be prefixed NEXT_PUBLIC_ so it is available in client-side code —
# the browser calls this API directly with its stored JWT.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
